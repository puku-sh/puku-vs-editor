name: Build Linux

on:
  push:
    tags:
      - 'v*'
    branches:
      - 'feat/ubunut'
  workflow_dispatch:
    inputs:
      architecture:
        description: 'Build architecture'
        required: true
        default: 'x64'
        type: choice
        options:
          - x64
          - arm64
  workflow_call:
    inputs:
      architecture:
        description: 'Build architecture'
        required: true
        type: string

jobs:
  build-linux-x64:
    name: Build Linux (x64)
    runs-on: self-hosted  # 🚀 Namespace.so 16 vCPU, 32GB RAM
    if: ${{ github.event_name != 'workflow_dispatch' || github.event.inputs.architecture == 'x64' }}
    env:
      VSCODE_ARCH: x64
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: 'src/vscode/.nvmrc'
          cache: 'npm'
          cache-dependency-path: |
            src/chat/package-lock.json
            src/vscode/package-lock.json

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            libx11-dev \
            libxkbfile-dev \
            libsecret-1-dev \
            libkrb5-dev \
            rpm \
            fakeroot \
            dpkg

      # 🚀 Cache node_modules
      - name: Cache node_modules
        id: cache-node-modules
        uses: actions/cache@v4
        with:
          path: |
            src/chat/node_modules
            src/vscode/node_modules
          key: ${{ runner.os }}-x64-node-modules-${{ hashFiles('src/chat/package-lock.json', 'src/vscode/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-x64-node-modules-

      # 🚀 Cache VS Code build output (saves 30-60 minutes)
      - name: Cache VS Code Build
        uses: actions/cache@v4
        with:
          path: |
            src/vscode/out
            src/vscode/out-build
            src/vscode/.build/extensions
            src/vscode/.build/builtInExtensions
          key: ${{ runner.os }}-x64-vscode-build-${{ hashFiles('src/vscode/src/**/*.ts', 'src/vscode/build/**/*.ts', 'src/vscode/product.json') }}
          restore-keys: |
            ${{ runner.os }}-x64-vscode-build-

      - name: Install dependencies
        if: steps.cache-node-modules.outputs.cache-hit != 'true'
        run: |
          set -e
          echo "=== Installing Extension Dependencies ==="
          cd src/chat

          # Clear npm cache and use legacy peer deps to avoid conflicts
          npm cache clean --force

          for i in {1..5}; do
            echo "Extension npm install attempt $i/5..."
            timeout 300 npm ci --legacy-peer-deps --no-audit --no-fund && break
            if [ $i -eq 5 ]; then
              echo "Extension npm install failed after 5 attempts" >&2
              echo "Last install log:"
              npm ci --legacy-peer-deps --no-audit --no-fund --verbose || true
              exit 1
            fi
            echo "Npm install failed, retry $i..."
            sleep 5
            npm cache clean --force
          done

          echo "=== Installing VS Code Dependencies ==="
          cd ../vscode

          # Clear npm cache
          npm cache clean --force

          for i in {1..5}; do
            echo "VS Code npm install attempt $i/5..."
            timeout 600 npm ci --legacy-peer-deps --no-audit --no-fund && break
            if [ $i -eq 5 ]; then
              echo "VS Code npm install failed after 5 attempts" >&2
              echo "Last install log:"
              npm ci --legacy-peer-deps --no-audit --no-fund --verbose || true
              exit 1
            fi
            echo "Npm install failed, retry $i..."
            sleep 5
            npm cache clean --force
          done
        env:
          ELECTRON_SKIP_BINARY_DOWNLOAD: 1
          PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD: 1
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          npm_config_loglevel: warn

      # 🚀 Build with optimizations
      - name: Build using Makefile
        run: make build
        env:
          VSCODE_ARCH: x64
          UV_THREADPOOL_SIZE: 128
          VSCODE_USE_ESBUILD: 1
          NODE_OPTIONS: --max-old-space-size=8192

      # 🆕 Create VS Code Linux Production Build
      - name: Create VS Code Linux Production Build
        run: |
          cd src/vscode
          echo "📦 Creating VS Code Linux production build using gulp..."

          # Run the gulp production build that creates the expected directory structure
          # Increased memory limit to prevent OOM errors
          NODE_OPTIONS="--max-old-space-size=8192" npx gulp vscode-linux-x64

          # Check if the production build was created successfully
          if [ -d "../VSCode-linux-x64" ]; then
            echo "✅ VS Code Linux production build created successfully!"
            echo "📍 Location: $(pwd)/../VSCode-linux-x64"
            echo "📊 Contents: $(ls -la ../VSCode-linux-x64 | head -10)"
          else
            echo "❌ VS Code Linux production build failed!"
            echo "Expected directory: ../VSCode-linux-x64 not found"
            exit 1
          fi
        env:
          VSCODE_ARCH: x64
          NODE_OPTIONS: --max-old-space-size=8192

      - name: Package Linux builds
        run: |
          chmod +x ./build-linux-optimized.sh
          ./build-linux-optimized.sh
        env:
          VSCODE_ARCH: x64

      - name: Upload Linux artifacts
        uses: actions/upload-artifact@v4
        with:
          name: puku-editor-linux-x64-${{ github.run_id }}
          path: |
            *.deb
            *.rpm
            *.tar.gz
          retention-days: 1
          if-no-files-found: error

      - name: Upload build logs
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: build-logs-linux-x64-${{ github.run_attempt }}
          path: |
            src/vscode/.build/logs
            src/chat/dist
          if-no-files-found: ignore
          retention-days: 7

  build-linux-arm64:
    name: Build Linux (ARM64)
    runs-on: self-hosted # 🚀 Namespace.so AmpereOne 16 vCPU, 32GB RAM
    if: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.architecture == 'arm64' }}
    env:
      VSCODE_ARCH: arm64
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: 'src/vscode/.nvmrc'
          cache: 'npm'
          cache-dependency-path: |
            src/chat/package-lock.json
            src/vscode/package-lock.json

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            libx11-dev \
            libxkbfile-dev \
            libsecret-1-dev \
            libkrb5-dev \
            rpm \
            fakeroot \
            dpkg \
            gcc-aarch64-linux-gnu \
            g++-aarch64-linux-gnu

      # 🚀 Cache node_modules
      - name: Cache node_modules
        id: cache-node-modules
        uses: actions/cache@v4
        with:
          path: |
            src/chat/node_modules
            src/vscode/node_modules
          key: ${{ runner.os }}-arm64-node-modules-${{ hashFiles('src/chat/package-lock.json', 'src/vscode/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-arm64-node-modules-

      # 🚀 Cache VS Code build output (saves 30-60 minutes)
      - name: Cache VS Code Build
        uses: actions/cache@v4
        with:
          path: |
            src/vscode/out
            src/vscode/out-build
            src/vscode/.build/extensions
            src/vscode/.build/builtInExtensions
          key: ${{ runner.os }}-arm64-vscode-build-${{ hashFiles('src/vscode/src/**/*.ts', 'src/vscode/build/**/*.ts', 'src/vscode/product.json') }}
          restore-keys: |
            ${{ runner.os }}-arm64-vscode-build-

      - name: Install dependencies
        if: steps.cache-node-modules.outputs.cache-hit != 'true'
        timeout-minutes: 30
        run: |
          set -e
          echo "=== Installing Extension Dependencies ==="
          cd src/chat

          # Clear npm cache and use legacy peer deps to avoid conflicts
          npm cache clean --force

          for i in {1..5}; do
            echo "Extension npm install attempt $i/5..."
            timeout 300 npm ci --legacy-peer-deps --no-audit --no-fund && break
            if [ $i -eq 5 ]; then
              echo "Extension npm install failed after 5 attempts" >&2
              echo "Last install log:"
              npm ci --legacy-peer-deps --no-audit --no-fund --verbose || true
              exit 1
            fi
            echo "Npm install failed, retry $i..."
            sleep 5
            npm cache clean --force
          done

          echo "=== Installing VS Code Dependencies ==="
          cd ../vscode

          # Clear npm cache
          npm cache clean --force

          for i in {1..5}; do
            echo "VS Code npm install attempt $i/5..."
            timeout 600 npm ci --legacy-peer-deps --no-audit --no-fund --arch=arm64 && break
            if [ $i -eq 5 ]; then
              echo "VS Code npm install failed after 5 attempts" >&2
              echo "Last install log:"
              npm ci --legacy-peer-deps --no-audit --no-fund --arch=arm64 --verbose || true
              exit 1
            fi
            echo "Npm install failed, retry $i..."
            sleep 5
            npm cache clean --force
          done
        env:
          npm_config_arch: arm64
          ELECTRON_SKIP_BINARY_DOWNLOAD: 1
          PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD: 1
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          npm_config_loglevel: warn

      # 🚀 Build with optimizations
      - name: Build using Makefile
        run: make build
        env:
          VSCODE_ARCH: arm64
          UV_THREADPOOL_SIZE: 128
          VSCODE_USE_ESBUILD: 1
          NODE_OPTIONS: --max-old-space-size=8192

      # 🆕 Create VS Code Linux Production Build
      - name: Create VS Code Linux Production Build
        run: |
          cd src/vscode
          echo "📦 Creating VS Code Linux production build using gulp..."

          # Run the gulp production build that creates the expected directory structure
          # Increased memory limit to prevent OOM errors
          NODE_OPTIONS="--max-old-space-size=8192" npx gulp vscode-linux-arm64

          # Check if the production build was created successfully
          if [ -d "../VSCode-linux-arm64" ]; then
            echo "✅ VS Code Linux ARM64 production build created successfully!"
            echo "📍 Location: $(pwd)/../VSCode-linux-arm64"
            echo "📊 Contents: $(ls -la ../VSCode-linux-arm64 | head -10)"
          else
            echo "❌ VS Code Linux ARM64 production build failed!"
            echo "Expected directory: ../VSCode-linux-arm64 not found"
            exit 1
          fi
        env:
          VSCODE_ARCH: arm64
          NODE_OPTIONS: --max-old-space-size=8192

      - name: Package Linux builds
        run: |
          chmod +x ./build-linux-optimized.sh
          ./build-linux-optimized.sh
        env:
          VSCODE_ARCH: arm64

      - name: Upload Linux artifacts
        uses: actions/upload-artifact@v4
        with:
          name: puku-editor-linux-arm64-${{ github.run_id }}
          path: |
            *.deb
            *.rpm
            *.tar.gz
          retention-days: 1
          if-no-files-found: error

      - name: Upload build logs
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: build-logs-linux-arm64-${{ github.run_attempt }}
          path: |
            src/vscode/.build/logs
            src/chat/dist
          if-no-files-found: ignore
          retention-days: 7
